{
  "name": "agent-budget",
  "version": "0.1.0",
  "description": "Local-first context and cost gateway for AI coding agents.",
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "bin": {
    "agent-budget": "dist/src/cli/index.js"
  },
  "files": [
    ".agents",
    "dist/hooks",
    "dist/src",
    "docs",
    "codex",
    "plugins",
    "skills",
    "hooks",
    "agent-budget.config.example.json",
    "AGENTS.example.md",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "agent-budget": "node dist/src/cli/index.js",
    "test": "vitest run tests/unit",
    "test:watch": "vitest",
    "e2e": "vitest run tests/e2e",
    "demo:fixture": "pnpm build && node dist/src/cli/index.js index --repo fixtures/react-ts-app && node dist/src/cli/index.js dossier \"Fix stale chart tooltip value after sensor reconnect\" --repo fixtures/react-ts-app --format markdown --budget 12000 > proof/sample-dossier.md && node dist/src/cli/index.js run --repo fixtures/react-ts-app --kind test -- pnpm test",
    "proof": "pnpm build && pnpm test && pnpm e2e && pnpm demo:fixture && node dist/src/cli/index.js proof --repo fixtures/react-ts-app"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "commander": "^12.1.0",
    "execa": "^9.5.2",
    "fast-glob": "^3.3.3",
    "ts-morph": "^23.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.8.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
