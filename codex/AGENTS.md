# Agent Budget workflow

Use the Agent Budget MCP tools before broad repo exploration. Start with `abg_repo_dossier`, then targeted `abg_read_budgeted` calls, and run tests through `abg_run_summary`.
