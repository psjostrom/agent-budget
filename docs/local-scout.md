# Local Scout

`fl_local_scout` is an optional extension point for a future local model or heuristic helper. It is disabled by default:

```json
{
  "localScout": {
    "enabled": false,
    "command": null
  }
}
```

v0.1 does not require Ollama, a paid LLM API, or any external model.
